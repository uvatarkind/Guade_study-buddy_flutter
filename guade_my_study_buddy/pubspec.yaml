name: guade_my_study_buddy
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0 # Check pub.dev for the latest version
  permission_handler: ^11.3.1 
  flutter_dotenv: ^5.1.0
  image_picker: ^1.0.7
  shared_preferences: ^2.2.2
  provider: ^6.1.5
  http: ^1.3.0
  table_calendar: ^3.0.9 # Use the latest version
  fl_chart: ^0.68.0 # Use the latest version
  flutter_svg: ^2.0.10+1 # For SVG icons, if you use them

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - .env 
    - assets/images/