name: guade_my_study_buddy
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  image_picker: ^1.0.7 # Replace with the actual latest version you want to use
  shared_preferences: ^2.2.2
  flutter:
    sdk: flutter
  provider: ^6.1.5
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
